[project]
name = "frappe_painter"
version = "1.0.0"
description = "No-code UI theme customizer for Frappe/ERPNext"

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
